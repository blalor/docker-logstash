filter {
    if [input] == "lumberjack" {
        mutate {
            ## not valuable
            remove_field => [ "offset" ]
            
            ## lumberjack or logstash-forwarder doesn't support an array of tags
            ## natively
            split => [ "tags", "," ]
        }
    }
}
